---
name: usedcar-report-html
description: 여러 사이트에서 수집·정규화한 중고차 후보를 입력받아, 조건 충족·DCT·사고/렌트·색상·옵션을 한눈에 비교하고 TOP 추천을 제시하는 정적 HTML 리포트를 생성한다(필요 시 Vercel 등으로 배포). "비교표 만들어줘/추천 웹 만들어줘" 요청에 사용.
---

# 중고차 종합 비교·추천 HTML 생성 스킬

수집 스킬(encar/kcar/kia-cpo/heydealer)의 정규화 결과 배열을 받아, 단일 `index.html`
(무의존·정적)로 **TOP 3 추천 카드 + 전체 비교표 + 방법론 노트**를 만든다.

## 입력 데이터 스키마 (사이트 공통 정규화)
각 매물 객체. **개인정보 금지 필드는 받지도 말 것**(PRIVACY.md). 매물 링크는 공개
상세 URL만.
```json
{
  "site": "엔카|케이카|기아CPO|헤이딜러|현대인증",
  "model": "투싼 NX4 인스퍼레이션",
  "trim": "인스퍼레이션",
  "color": "흰색",
  "price_manwon": 2499,
  "year": "2022.06",
  "km": 30760,
  "fuel": "가솔린|디젤|하이브리드",
  "transmission": "7DCT건식|8DCT습식|8AT|6AT(HEV)",
  "dct": "건식7|습식8|무DCT",
  "options": {"후방카메라":true,"내비":true,"ADAS":true,"통풍시트":true,"서라운드뷰":true,"선루프":false,"HUD":false},
  "accident": "무사고|단순교환무사고|사고",
  "insurance": "전기간가입|미가입N구간|내차Nmanwon/타차Nmanwon",
  "rental": "없음|렌트|확인필요",
  "url": "https://...(공개 상세 페이지)"
}
```

## 생성 규칙
- **필수옵션 5종**(후방카메라·대화면내비·ADAS·통풍시트·서라운드뷰) 충족 여부 표기.
- **DCT 경고 박스**: 건식7DCT(가솔린1.6T)=저속 울컥 심함, 습식8DCT(디젤)=완화,
  무DCT(가솔린2.0/2.5·HEV)=무관.
- **흰색 우선 정렬**, 렌트/보험 미가입은 별도 구간으로 분리.
- TOP 3는 색상·이력·DCT·옵션 종합 점수로 선정하되 근거를 카드에 명시.
- **랭킹 가중치(권장)**: 패널티(낮을수록 상위) = `DCT위험*3 + (HEV면 0 아니면 가점차) +
  내차피해(백만원) - 옵션수(SVM·선루프)`. "비슷한 가격이면 하이브리드 선호"는 HEV에
  가점을 줘 반영하되, **사고피해·옵션도 함께 반영**해 피해 큰 HEV가 깨끗한 차를 무조건
  앞서지 않게 한다. 하이브리드/무DCT만 보는 필터 컬럼을 제공하면 좋다.
- **하이브리드=무DCT**(현대 A6MF2H/TMED 6단 자동, DCT 아님) → 울컥 경고에서 제외하고
  "하이브리드만" 필터를 둔다. 전수 개별검증 결과를 입력으로 쓰면 사고/렌트/옵션이 실측이라
  추천 신뢰도가 높다(encar 스킬 3) 참고).
- **CVT(QM6 등)**도 DCT 아님 → 울컥 무관. 변속기 라벨에 `DCT` 문자열을 넣지 말 것
  (「무DCT만」 필터가 `/DCT/` 매칭으로 잘못 제외함). `CVT`로만 표기하고 무DCT급으로 정렬.

## 총소유비용(TCO) 추정 — 과장 없이, 근거 있게
연료비만이 아니라 **차종별로 달라지는 비용**을 합산해 "5년 실부담"을 보여주면 신뢰도가 높다.
모든 수치는 **추정**임을 명시하고, 검증 가능한 공식 수치는 그대로 쓴다.

- **연료비**: **공인 복합연비**(검증 가능) 기준으로 계산해 과장을 막는다. 도심·실연비를
  쓰면 가솔린에 불리하게 부풀려질 수 있다(주행패턴은 주석으로). 휘발유/경유 단가 명시.
- **자동차세**(법정): 배기량 cc × 단가(≤1,600cc 140원·초과 200원) × 1.3(지방교육세).
  차령 3년부터 매년 5%씩 경감(12년 50% 한도) → 5년 보유분은 연세액×~3.75(5년차 기준).
  → 1.6T·하이브리드(1,598cc) ≈ 연 29만 vs 2.5(2,497cc) ≈ 연 65만으로 **차이 큼**.
- **취득세**(법정): 중고 승용 7%(하이브리드 감면은 2024년말 폐지). 과표=실거래가/시가표준 중 높은 값.
- **보험료**(추정): 연령(나이특약)·무사고·자차포함 가정. 자차는 차값에 비례(≈차값×1.4%).
  차 노후로 매년 소폭↓ → 5년 ≈ 연×4.3.
- **정비 리스크**(차종별 추정): 건식7DCT 가솔린=클러치/플라이휠 ~150만, 디젤=DPF 등 ~80만,
  하이브리드=회생제동 브레이크 절감 ~30만, 무DCT/CVT 가솔린 ~40만.
- **5년뒤 재판매**(추정): 현재 중고가 × 잔존율. 5년 더 지나면 ≈10년·15만km+라 **현재가의
  38~45%**. 국산 SUV(싼타페/스포티지) 강세, 르노·디젤·구형 하이브리드(배터리)는 낮게.
- **실부담(5년)** = 차값+취득세+5년(연료+보험+자동차세+정비) − 재판매가. **이게 진짜 비교 기준**.
- 표 컬럼은 머리글 클릭 **다중정렬**(선호도=가격·비용 낮을수록·옵션 많을수록 위)과
  기본 정렬(예: 실부담 1순위, 옵션 2순위)·기본 필터(렌트제외·건식DCT제외)를 제공하면 좋다.
- **PII 절대 미포함**(차량번호·연락처·딜러명). 링크는 공개 URL만.

## 산출/배포
- `scripts/render.js`(Node, 무의존)로 데이터 JSON → `index.html` 생성.
  `node scripts/render.js data.json > index.html`
- 배포는 선택: `vercel deploy --prod`(정적). 데이터 JSON은 저장소에 커밋하지 않는다.

## 주의
- 입력 데이터는 사용자 기기의 일회성 산출물. HTML도 개인 검토용. 공개 배포 시
  매물 데이터의 약관/DB권/개인정보 이슈를 이용자가 확인할 책임.
